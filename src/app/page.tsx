import { BridgeWidget } from "~/components/bridge/bridge-widget";

export default function HomePage() {
  return (
    <main className="flex h-full flex-col items-center gap-4 pt-24">
      <BridgeWidget />
    </main>
  );
}
